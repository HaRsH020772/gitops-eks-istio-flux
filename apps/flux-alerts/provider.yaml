apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: slack
  namespace: flux-system
spec:
  type: slack
  # Webhook URL comes from the SOPS-encrypted secret (key: address).
  secretRef:
    name: slack-webhook
