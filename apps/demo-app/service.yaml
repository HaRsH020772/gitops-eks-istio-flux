apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: demo-app
  labels:
    app: demo-app
spec:
  selector:
    app: demo-app
  ports:
    # Port name "http" doubles as Istio's protocol hint.
    - name: http
      port: 80
      targetPort: http
