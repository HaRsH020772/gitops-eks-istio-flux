# Mesh-wide strict mTLS: every sidecar-injected workload only accepts
# mutual-TLS traffic from other mesh members.
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
spec:
  mtls:
    mode: STRICT
